<!DOCTYPE html>
<html lang="ru">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
<title>Дни рождения</title>
<script src="https://telegram.org/js/telegram-web-app.js"></script>
<style>
  :root {
    --bg: #0f0f13;
    --surface: #1a1a24;
    --surface2: #22222f;
    --border: rgba(255,255,255,0.07);
    --accent: #f5b942;
    --accent-dim: rgba(245,185,66,0.12);
    --accent-soft: rgba(245,185,66,0.22);
    --text: #f0f0f5;
    --text-muted: rgba(240,240,245,0.45);
    --text-mid: rgba(240,240,245,0.7);
    --red: #e05b5b;
    --green: #5be08a;
    --radius: 16px;
    --radius-sm: 10px;
  }

  * { box-sizing: border-box; margin: 0; padding: 0; -webkit-tap-highlight-color: transparent; }

  body {
    font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
    background: var(--bg);
    color: var(--text);
    min-height: 100vh;
    overflow-x: hidden;
  }

  /* ── SCREENS ─────────────────────────────── */
  .screen { display: none; flex-direction: column; min-height: 100vh; }
  .screen.active { display: flex; }

  /* ── HEADER ──────────────────────────────── */
  .header {
    padding: 20px 20px 12px;
    display: flex;
    align-items: center;
    gap: 12px;
  }
  .header-back {
    width: 36px; height: 36px;
    border-radius: 10px;
    background: var(--surface);
    border: 1px solid var(--border);
    display: flex; align-items: center; justify-content: center;
    cursor: pointer; flex-shrink: 0;
    transition: background 0.15s;
  }
  .header-back:active { background: var(--surface2); }
  .header-title { font-size: 17px; font-weight: 600; flex: 1; }
  .header-action {
    font-size: 14px; font-weight: 500;
    color: var(--accent);
    cursor: pointer;
    padding: 6px 2px;
  }

  /* ── LIST SCREEN ─────────────────────────── */
  .list-top {
    padding: 24px 20px 16px;
  }
  .list-title {
    font-size: 26px;
    font-weight: 700;
    letter-spacing: -0.5px;
  }
  .list-subtitle {
    margin-top: 4px;
    font-size: 14px;
    color: var(--text-muted);
  }

  /* Nearest birthday banner */
  .next-banner {
    margin: 0 20px 16px;
    background: var(--accent-dim);
    border: 1px solid var(--accent-soft);
    border-radius: var(--radius);
    padding: 14px 16px;
    display: flex;
    align-items: center;
    gap: 12px;
  }
  .next-banner-emoji { font-size: 28px; flex-shrink: 0; }
  .next-banner-info { flex: 1; min-width: 0; }
  .next-banner-name { font-size: 15px; font-weight: 600; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
  .next-banner-days { margin-top: 2px; font-size: 13px; color: var(--accent); font-weight: 500; }

  /* Search */
  .search-wrap { padding: 0 20px 12px; }
  .search-input {
    width: 100%;
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: var(--radius-sm);
    padding: 10px 14px 10px 36px;
    color: var(--text);
    font-size: 15px;
    outline: none;
    font-family: inherit;
  }
  .search-input::placeholder { color: var(--text-muted); }
  .search-wrap { position: relative; }
  .search-icon {
    position: absolute;
    left: 32px; top: 50%;
    transform: translateY(-50%);
    opacity: 0.35;
    pointer-events: none;
  }

  /* People list */
  .people-list { flex: 1; padding: 0 20px; display: flex; flex-direction: column; gap: 10px; }

  .person-card {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    padding: 14px 16px;
    display: flex;
    align-items: center;
    gap: 14px;
    cursor: pointer;
    transition: background 0.12s;
    position: relative;
  }
  .person-card:active { background: var(--surface2); }

  .person-avatar {
    width: 44px; height: 44px;
    border-radius: 50%;
    display: flex; align-items: center; justify-content: center;
    font-size: 18px;
    font-weight: 700;
    flex-shrink: 0;
  }

  .person-info { flex: 1; min-width: 0; }
  .person-name { font-size: 15px; font-weight: 600; white-space: nowrap; overflow: hidden; text-overflow: ellipsis; }
  .person-date { font-size: 13px; color: var(--text-mid); margin-top: 2px; }

  .person-badge {
    flex-shrink: 0;
    text-align: right;
  }
  .badge-days {
    font-size: 22px;
    font-weight: 700;
    line-height: 1;
  }
  .badge-label {
    font-size: 11px;
    color: var(--text-muted);
    margin-top: 2px;
  }
  .badge-today { color: var(--accent); }
  .badge-soon { color: #f5a742; }
  .badge-normal { color: var(--text-mid); }

  /* Urgency strip */
  .urgency-strip {
    position: absolute;
    top: 0; left: 0; right: 0;
    height: 3px;
    border-radius: var(--radius) var(--radius) 0 0;
  }

  /* Sections */
  .section-label {
    font-size: 12px;
    font-weight: 600;
    color: var(--text-muted);
    text-transform: uppercase;
    letter-spacing: 0.06em;
    margin: 16px 0 8px;
    padding: 0 20px;
  }

  /* FAB */
  .fab {
    position: fixed;
    right: 20px; bottom: 28px;
    width: 56px; height: 56px;
    background: var(--accent);
    border-radius: 50%;
    display: flex; align-items: center; justify-content: center;
    cursor: pointer;
    box-shadow: 0 4px 20px rgba(245,185,66,0.4);
    transition: transform 0.15s, box-shadow 0.15s;
    z-index: 100;
  }
  .fab:active { transform: scale(0.93); box-shadow: 0 2px 10px rgba(245,185,66,0.3); }
  .fab svg { color: #0f0f13; }

  .list-bottom-pad { height: 90px; }

  /* Empty state */
  .empty-state {
    flex: 1;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 12px;
    padding: 40px 30px;
    text-align: center;
  }
  .empty-emoji { font-size: 52px; }
  .empty-title { font-size: 18px; font-weight: 600; }
  .empty-sub { font-size: 14px; color: var(--text-muted); line-height: 1.5; }

  /* ── FORM SCREEN ─────────────────────────── */
  .form-body { flex: 1; padding: 8px 20px 24px; overflow-y: auto; }

  .form-section { margin-bottom: 24px; }
  .form-section-label {
    font-size: 12px;
    font-weight: 600;
    color: var(--text-muted);
    text-transform: uppercase;
    letter-spacing: 0.06em;
    margin-bottom: 10px;
  }

  .field {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: var(--radius-sm);
    margin-bottom: 8px;
    overflow: hidden;
    transition: border-color 0.15s;
  }
  .field:focus-within { border-color: var(--accent-soft); }

  .field-label {
    font-size: 11px;
    font-weight: 500;
    color: var(--text-muted);
    padding: 10px 14px 0;
    letter-spacing: 0.02em;
  }
  .field input, .field textarea, .field select {
    width: 100%;
    background: transparent;
    border: none;
    outline: none;
    color: var(--text);
    font-size: 15px;
    font-family: inherit;
    padding: 6px 14px 10px;
  }
  .field textarea {
    resize: none;
    min-height: 80px;
  }
  .field select { appearance: none; cursor: pointer; }
  .field select option { background: #22222f; }

  /* Notify toggles */
  .notify-options { display: flex; flex-direction: column; gap: 8px; }
  .notify-option {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: var(--radius-sm);
    padding: 13px 16px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    cursor: pointer;
    transition: border-color 0.15s;
  }
  .notify-option.selected {
    border-color: var(--accent-soft);
    background: var(--accent-dim);
  }
  .notify-option-left { display: flex; align-items: center; gap: 10px; }
  .notify-option-icon { font-size: 20px; }
  .notify-option-text { font-size: 14px; font-weight: 500; }
  .notify-option-sub { font-size: 12px; color: var(--text-muted); margin-top: 1px; }
  .notify-check {
    width: 22px; height: 22px;
    border-radius: 50%;
    border: 2px solid var(--border);
    display: flex; align-items: center; justify-content: center;
    transition: all 0.15s;
  }
  .notify-option.selected .notify-check {
    background: var(--accent);
    border-color: var(--accent);
  }

  /* Save button */
  .save-btn {
    width: 100%;
    background: var(--accent);
    color: #0f0f13;
    border: none;
    border-radius: var(--radius-sm);
    padding: 15px;
    font-size: 16px;
    font-weight: 600;
    font-family: inherit;
    cursor: pointer;
    margin-top: 8px;
    transition: opacity 0.15s;
  }
  .save-btn:active { opacity: 0.85; }

  .delete-btn {
    width: 100%;
    background: transparent;
    color: var(--red);
    border: 1px solid rgba(224,91,91,0.25);
    border-radius: var(--radius-sm);
    padding: 13px;
    font-size: 15px;
    font-weight: 500;
    font-family: inherit;
    cursor: pointer;
    margin-top: 8px;
    transition: background 0.15s;
  }
  .delete-btn:active { background: rgba(224,91,91,0.08); }

  /* ── DETAIL SCREEN ───────────────────────── */
  .detail-hero {
    padding: 20px;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 14px;
  }
  .detail-avatar {
    width: 80px; height: 80px;
    border-radius: 50%;
    display: flex; align-items: center; justify-content: center;
    font-size: 34px;
    font-weight: 700;
  }
  .detail-name { font-size: 22px; font-weight: 700; text-align: center; }
  .detail-date { font-size: 15px; color: var(--text-mid); }

  .detail-countdown {
    margin: 0 20px 20px;
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    padding: 20px;
    text-align: center;
  }
  .countdown-number { font-size: 52px; font-weight: 800; line-height: 1; letter-spacing: -2px; }
  .countdown-label { font-size: 14px; color: var(--text-muted); margin-top: 6px; }
  .countdown-age { font-size: 13px; color: var(--text-mid); margin-top: 8px; }

  .detail-cards { padding: 0 20px; display: flex; flex-direction: column; gap: 10px; }
  .info-card {
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: var(--radius);
    padding: 14px 16px;
  }
  .info-card-label { font-size: 12px; color: var(--text-muted); font-weight: 500; margin-bottom: 6px; }
  .info-card-value { font-size: 15px; line-height: 1.5; }
  .info-card-empty { font-size: 14px; color: var(--text-muted); font-style: italic; }

  .detail-edit-btn {
    margin: 20px;
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: var(--radius-sm);
    padding: 13px;
    font-size: 15px;
    font-weight: 500;
    color: var(--text);
    font-family: inherit;
    cursor: pointer;
    width: calc(100% - 40px);
    transition: background 0.12s;
  }
  .detail-edit-btn:active { background: var(--surface2); }

  /* ── NOTIFY SCREEN ───────────────────────── */
  .notify-screen-body { flex: 1; padding: 8px 20px 24px; }
  .notify-title { font-size: 20px; font-weight: 700; padding: 16px 20px 8px; }
  .notify-sub { font-size: 14px; color: var(--text-muted); padding: 0 20px 20px; line-height: 1.5; }

  /* Tabs */
  .tabs {
    display: flex;
    gap: 6px;
    padding: 0 20px 16px;
  }
  .tab {
    flex: 1;
    background: var(--surface);
    border: 1px solid var(--border);
    border-radius: var(--radius-sm);
    padding: 8px;
    text-align: center;
    font-size: 13px;
    font-weight: 500;
    color: var(--text-muted);
    cursor: pointer;
    transition: all 0.15s;
  }
  .tab.active {
    background: var(--accent-dim);
    border-color: var(--accent-soft);
    color: var(--accent);
  }

  /* ── UTILS ───────────────────────────────── */
  .mt8 { margin-top: 8px; }
  .hidden { display: none !important; }

  @supports (padding-bottom: env(safe-area-inset-bottom)) {
    .fab { bottom: calc(28px + env(safe-area-inset-bottom)); }
  }
</style>
</head>
<body>

<!-- ═══════════════ LIST SCREEN ═══════════════ -->
<div class="screen active" id="screen-list">
  <div class="list-top">
    <div class="list-title">🎂 Дни рождения</div>
    <div class="list-subtitle" id="list-subtitle">Загрузка...</div>
  </div>

  <div id="next-banner" class="next-banner hidden">
    <div class="next-banner-emoji" id="banner-emoji">🎉</div>
    <div class="next-banner-info">
      <div class="next-banner-name" id="banner-name"></div>
      <div class="next-banner-days" id="banner-days"></div>
    </div>
  </div>

  <div class="search-wrap" style="position:relative;padding:0 20px 12px;">
    <svg class="search-icon" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
      <circle cx="11" cy="11" r="8"/><path d="m21 21-4.35-4.35"/>
    </svg>
    <input class="search-input" id="search-input" type="text" placeholder="Поиск..." oninput="renderList()">
  </div>

  <div class="tabs">
    <div class="tab active" onclick="setTab('all', this)">Все</div>
    <div class="tab" onclick="setTab('soon', this)">Скоро</div>
    <div class="tab" onclick="setTab('month', this)">Этот месяц</div>
  </div>

  <div id="people-list" class="people-list"></div>
  <div class="list-bottom-pad"></div>

  <div class="fab" onclick="openAdd()">
    <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round">
      <line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/>
    </svg>
  </div>
</div>

<!-- ═══════════════ FORM SCREEN ═══════════════ -->
<div class="screen" id="screen-form">
  <div class="header">
    <div class="header-back" onclick="goBack()">
      <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">
        <path d="M19 12H5M12 19l-7-7 7-7"/>
      </svg>
    </div>
    <div class="header-title" id="form-title">Добавить</div>
  </div>

  <div class="form-body">
    <div class="form-section">
      <div class="form-section-label">Основное</div>

      <div class="field">
        <div class="field-label">ИМЯ И ФАМИЛИЯ</div>
        <input type="text" id="f-name" placeholder="Алексей Учитель">
      </div>

      <div class="field">
        <div class="field-label">ДАТА РОЖДЕНИЯ</div>
        <input type="date" id="f-date" placeholder="дд.мм.гггг">
      </div>
      <div style="font-size:12px;color:var(--text-muted);padding:4px 2px 0;">
        Год нужен чтобы считать возраст — можно оставить любой, если не знаешь
      </div>
    </div>

    <div class="form-section">
      <div class="form-section-label">Подарок 🎁</div>
      <div class="field">
        <div class="field-label">ЧТО ПОДАРИТЬ</div>
        <textarea id="f-gift" placeholder="Запиши идею на будущее... можно оставить пустым"></textarea>
      </div>
    </div>

    <div class="form-section">
      <div class="form-section-label">Уведомления 🔔</div>
      <div class="notify-options" id="notify-options">
        <div class="notify-option selected" data-days="1" onclick="toggleNotify(this)">
          <div class="notify-option-left">
            <div class="notify-option-icon">🌅</div>
            <div>
              <div class="notify-option-text">В день рождения</div>
              <div class="notify-option-sub">Утром в сам день</div>
            </div>
          </div>
          <div class="notify-check">
            <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="#0f0f13" stroke-width="3" stroke-linecap="round"><polyline points="20 6 9 17 4 12"/></svg>
          </div>
        </div>
        <div class="notify-option" data-days="3" onclick="toggleNotify(this)">
          <div class="notify-option-left">
            <div class="notify-option-icon">📅</div>
            <div>
              <div class="notify-option-text">За 3 дня</div>
              <div class="notify-option-sub">Чтобы успеть подготовиться</div>
            </div>
          </div>
          <div class="notify-check">
            <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="#0f0f13" stroke-width="3" stroke-linecap="round"><polyline points="20 6 9 17 4 12"/></svg>
          </div>
        </div>
        <div class="notify-option" data-days="7" onclick="toggleNotify(this)">
          <div class="notify-option-left">
            <div class="notify-option-icon">📆</div>
            <div>
              <div class="notify-option-text">За неделю</div>
              <div class="notify-option-sub">Время купить подарок</div>
            </div>
          </div>
          <div class="notify-check">
            <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="#0f0f13" stroke-width="3" stroke-linecap="round"><polyline points="20 6 9 17 4 12"/></svg>
          </div>
        </div>
        <div class="notify-option" data-days="14" onclick="toggleNotify(this)">
          <div class="notify-option-left">
            <div class="notify-option-icon">🗓️</div>
            <div>
              <div class="notify-option-text">За 2 недели</div>
              <div class="notify-option-sub">Максимум времени на подготовку</div>
            </div>
          </div>
          <div class="notify-check">
            <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="#0f0f13" stroke-width="3" stroke-linecap="round"><polyline points="20 6 9 17 4 12"/></svg>
          </div>
        </div>
        <div class="notify-option" data-days="30" onclick="toggleNotify(this)">
          <div class="notify-option-left">
            <div class="notify-option-icon">🎯</div>
            <div>
              <div class="notify-option-text">За месяц</div>
              <div class="notify-option-sub">Планировщик</div>
            </div>
          </div>
          <div class="notify-check">
            <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="#0f0f13" stroke-width="3" stroke-linecap="round"><polyline points="20 6 9 17 4 12"/></svg>
          </div>
        </div>
      </div>
    </div>

    <button class="save-btn" onclick="savePerson()">Сохранить</button>
    <button class="delete-btn hidden" id="delete-btn" onclick="deletePerson()">Удалить</button>
  </div>
</div>

<!-- ═══════════════ DETAIL SCREEN ═══════════════ -->
<div class="screen" id="screen-detail">
  <div class="header">
    <div class="header-back" onclick="goBack()">
      <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round">
        <path d="M19 12H5M12 19l-7-7 7-7"/>
      </svg>
    </div>
    <div class="header-title">Профиль</div>
    <div class="header-action" onclick="openEdit()">Изменить</div>
  </div>

  <div class="detail-hero">
    <div class="detail-avatar" id="d-avatar"></div>
    <div>
      <div class="detail-name" id="d-name"></div>
      <div class="detail-date" id="d-date"></div>
    </div>
  </div>

  <div class="detail-countdown" id="d-countdown">
    <div class="countdown-number" id="d-days"></div>
    <div class="countdown-label" id="d-label"></div>
    <div class="countdown-age" id="d-age"></div>
  </div>

  <div class="detail-cards">
    <div class="info-card" id="d-gift-card">
      <div class="info-card-label">🎁 Что подарить</div>
      <div id="d-gift-value"></div>
    </div>
    <div class="info-card" id="d-notify-card">
      <div class="info-card-label">🔔 Уведомления</div>
      <div class="info-card-value" id="d-notify-value"></div>
    </div>
  </div>

  <button class="detail-edit-btn" onclick="openEdit()">✏️ Редактировать</button>
  <div style="height:24px"></div>
</div>

<script>
// ════════════════════════════════════════
//  DATA
// ════════════════════════════════════════
let people = JSON.parse(localStorage.getItem('bd_people') || '[]');
let editingId = null;
let currentTab = 'all';
let viewingId = null;

function save() {
  localStorage.setItem('bd_people', JSON.stringify(people));
}

function genId() {
  return Date.now().toString(36) + Math.random().toString(36).slice(2, 6);
}

// ════════════════════════════════════════
//  DATE UTILS
// ════════════════════════════════════════
function daysUntilBirthday(dateStr) {
  const today = new Date();
  today.setHours(0,0,0,0);
  const parts = dateStr.split('-').map(Number);
  const m = parts[1], d = parts[2];
  let next = new Date(today.getFullYear(), m-1, d);
  if (next < today) next.setFullYear(today.getFullYear()+1);
  return Math.round((next - today) / 86400000);
}

function formatDate(dateStr) {
  const parts = dateStr.split('-').map(Number);
  const y = parts[0], m = parts[1], d = parts[2];
  const months = ['января','февраля','марта','апреля','мая','июня',
                  'июля','августа','сентября','октября','ноября','декабря'];
  return y > 1 ? `${d} ${months[m-1]} ${y} г.р.` : `${d} ${months[m-1]}`;
}

function ageOnNextBirthday(dateStr) {
  const parts = dateStr.split('-').map(Number);
  const y = parts[0], m = parts[1], d = parts[2];
  if (!y || y < 2) return null;
  const today = new Date();
  let nextYear = today.getFullYear();
  const next = new Date(nextYear, m-1, d);
  if (next <= today) nextYear++;
  return nextYear - y;
}

function urgencyEmoji(days) {
  if (days === 0) return '🎂';
  if (days <= 3) return '🎉';
  if (days <= 7) return '🎈';
  if (days <= 14) return '⏳';
  if (days <= 30) return '📅';
  return '🗓️';
}

function urgencyColor(days) {
  if (days === 0) return '#f5b942';
  if (days <= 3) return '#f5b942';
  if (days <= 7) return '#f5a742';
  if (days <= 14) return '#8b7cf5';
  return null;
}

function avatarColor(name) {
  const colors = [
    ['#3b82f6','#1d4ed8'],['#8b5cf6','#6d28d9'],['#ec4899','#be185d'],
    ['#f59e0b','#b45309'],['#10b981','#047857'],['#ef4444','#b91c1c'],
    ['#06b6d4','#0e7490'],
  ];
  const idx = name.charCodeAt(0) % colors.length;
  return colors[idx];
}

function initials(name) {
  return name.trim().split(/\s+/).slice(0,2).map(w => w[0]).join('').toUpperCase();
}

function notifyLabel(days) {
  const map = {1:'в день рождения',3:'за 3 дня',7:'за неделю',14:'за 2 недели',30:'за месяц'};
  return map[days] || `за ${days} дней`;
}

// ════════════════════════════════════════
//  SCREEN NAV
// ════════════════════════════════════════
function showScreen(id) {
  document.querySelectorAll('.screen').forEach(s => s.classList.remove('active'));
  document.getElementById('screen-' + id).classList.add('active');
  window.scrollTo(0,0);
}

function goBack() {
  showScreen('list');
  editingId = null;
  viewingId = null;
  renderList();
}

// ════════════════════════════════════════
//  TABS
// ════════════════════════════════════════
function setTab(tab, el) {
  currentTab = tab;
  document.querySelectorAll('.tab').forEach(t => t.classList.remove('active'));
  el.classList.add('active');
  renderList();
}

// ════════════════════════════════════════
//  LIST
// ════════════════════════════════════════
function renderList() {
  const q = document.getElementById('search-input').value.toLowerCase();
  let filtered = people.map(p => ({ ...p, days: daysUntilBirthday(p.date) }));

  if (q) filtered = filtered.filter(p => p.name.toLowerCase().includes(q));
  if (currentTab === 'soon') filtered = filtered.filter(p => p.days <= 30);
  if (currentTab === 'month') {
    const today = new Date();
    filtered = filtered.filter(p => {
      const [, m] = p.date.split('-').map(Number);
      return m === today.getMonth()+1;
    });
  }

  filtered.sort((a,b) => a.days - b.days);

  // subtitle
  const sub = document.getElementById('list-subtitle');
  sub.textContent = people.length === 0 ? 'Добавь первого человека' :
    `${people.length} ${people.length===1?'человек':'человека'} в списке`;

  // banner
  const banner = document.getElementById('next-banner');
  if (people.length > 0 && !q && currentTab === 'all') {
    const next = [...people].map(p=>({...p,days:daysUntilBirthday(p.date)})).sort((a,b)=>a.days-b.days)[0];
    banner.classList.remove('hidden');
    document.getElementById('banner-emoji').textContent = urgencyEmoji(next.days);
    document.getElementById('banner-name').textContent = next.name;
    document.getElementById('banner-days').textContent = next.days === 0
      ? '🎉 Сегодня!' : `Через ${next.days} ${dayWord(next.days)}`;
  } else {
    banner.classList.add('hidden');
  }

  const container = document.getElementById('people-list');
  container.innerHTML = '';

  if (filtered.length === 0) {
    container.innerHTML = `
      <div class="empty-state">
        <div class="empty-emoji">${q ? '🔍' : currentTab==='soon' ? '✅' : '📆'}</div>
        <div class="empty-title">${q ? 'Никого не нашли' : currentTab==='soon' ? 'Скоро никого нет' : 'Здесь пусто'}</div>
        <div class="empty-sub">${q ? 'Попробуй другой запрос' : 'Добавь первого человека,\nнажав + внизу'}</div>
      </div>`;
    return;
  }

  // group today
  const todayPeople = filtered.filter(p => p.days === 0);
  const soonPeople = filtered.filter(p => p.days > 0 && p.days <= 7);
  const restPeople = filtered.filter(p => p.days > 7);

  function renderCard(p) {
    const [bg, dark] = avatarColor(p.name);
    const days = p.days;
    const color = urgencyColor(days);
    const badgeClass = days===0 ? 'badge-today' : days<=7 ? 'badge-soon' : 'badge-normal';
    const daysText = days===0 ? '🎂' : days;
    const daysLabel = days===0 ? 'СЕГОДНЯ!' : dayWord(days);
    const strip = color ? `<div class="urgency-strip" style="background:linear-gradient(90deg,${color}88,transparent)"></div>` : '';

    const div = document.createElement('div');
    div.className = 'person-card';
    div.onclick = () => openDetail(p.id);
    div.innerHTML = `
      ${strip}
      <div class="person-avatar" style="background:${bg}22;color:${bg};border:2px solid ${bg}44">${initials(p.name)}</div>
      <div class="person-info">
        <div class="person-name">${p.name}</div>
        <div class="person-date">${formatDate(p.date)}</div>
      </div>
      <div class="person-badge">
        <div class="badge-days ${badgeClass}">${daysText}</div>
        <div class="badge-label">${daysLabel}</div>
      </div>`;
    container.appendChild(div);
  }

  if (todayPeople.length && currentTab==='all' && !q) {
    const lbl = document.createElement('div');
    lbl.className='section-label'; lbl.textContent='🎂 Сегодня именинники!';
    container.appendChild(lbl);
    todayPeople.forEach(renderCard);
  }
  if (soonPeople.length && currentTab==='all' && !q) {
    const lbl = document.createElement('div');
    lbl.className='section-label'; lbl.textContent='🎉 На этой неделе';
    container.appendChild(lbl);
    soonPeople.forEach(renderCard);
  }
  if (restPeople.length && currentTab==='all' && !q) {
    if (todayPeople.length || soonPeople.length) {
      const lbl = document.createElement('div');
      lbl.className='section-label'; lbl.textContent='📋 Все остальные';
      container.appendChild(lbl);
    }
    restPeople.forEach(renderCard);
  }
  if (q || currentTab !== 'all') {
    filtered.forEach(renderCard);
  }
}

function dayWord(n) {
  if (n===1) return 'день';
  if (n>=2&&n<=4) return 'дня';
  return 'дней';
}

// ════════════════════════════════════════
//  ADD / EDIT FORM
// ════════════════════════════════════════
function openAdd() {
  editingId = null;
  document.getElementById('form-title').textContent = 'Добавить';
  document.getElementById('f-name').value = '';
  document.getElementById('f-date').value = '';
  document.getElementById('f-year').value = '';
  document.getElementById('f-gift').value = '';
  document.getElementById('delete-btn').classList.add('hidden');
  // reset notify — only day-of selected
  document.querySelectorAll('.notify-option').forEach(el => {
    el.classList.toggle('selected', el.dataset.days === '1');
  });
  showScreen('form');
  setTimeout(() => document.getElementById('f-name').focus(), 300);
}

function openEdit() {
  const p = people.find(x => x.id === viewingId);
  if (!p) return;
  editingId = p.id;
  document.getElementById('form-title').textContent = 'Изменить';
  document.getElementById('f-name').value = p.name;
  document.getElementById('f-date').value = p.date;
  document.getElementById('f-gift').value = p.gift || '';
  document.getElementById('delete-btn').classList.remove('hidden');
  const sel = p.notify || [1];
  document.querySelectorAll('.notify-option').forEach(el => {
    el.classList.toggle('selected', sel.includes(Number(el.dataset.days)));
  });
  showScreen('form');
}

function toggleNotify(el) {
  el.classList.toggle('selected');
}

function savePerson() {
  const name = document.getElementById('f-name').value.trim();
  const date = document.getElementById('f-date').value;
  if (!name || !date) {
    alert('Введи имя и дату рождения');
    return;
  }
  const gift = document.getElementById('f-gift').value.trim();
  const notify = [...document.querySelectorAll('.notify-option.selected')]
    .map(el => Number(el.dataset.days));
  if (notify.length === 0) notify.push(1);

  if (editingId) {
    const idx = people.findIndex(p => p.id === editingId);
    people[idx] = { id: editingId, name, date, gift, notify };
  } else {
    people.push({ id: genId(), name, date, gift, notify });
  }
  save();

  // notify bot (if in TG)
  tgSendData({ action: editingId ? 'update' : 'add', name, date: stored, notify });

  showScreen('list');
  editingId = null;
  renderList();
}

function deletePerson() {
  if (!confirm(`Удалить ${people.find(p=>p.id===editingId)?.name}?`)) return;
  people = people.filter(p => p.id !== editingId);
  save();
  editingId = null;
  viewingId = null;
  showScreen('list');
  renderList();
}

// ════════════════════════════════════════
//  DETAIL
// ════════════════════════════════════════
function openDetail(id) {
  const p = people.find(x => x.id === id);
  if (!p) return;
  viewingId = id;

  const [bg] = avatarColor(p.name);
  const el = document.getElementById('d-avatar');
  el.textContent = initials(p.name);
  el.style.background = bg + '22';
  el.style.color = bg;
  el.style.border = `2px solid ${bg}44`;

  document.getElementById('d-name').textContent = p.name;
  document.getElementById('d-date').textContent = formatDate(p.date);

  const days = daysUntilBirthday(p.date);
  const dEl = document.getElementById('d-days');
  const lEl = document.getElementById('d-label');
  const aEl = document.getElementById('d-age');

  if (days === 0) {
    dEl.textContent = '🎂';
    dEl.style.fontSize = '52px';
    lEl.textContent = 'Сегодня день рождения! 🎉';
    lEl.style.color = 'var(--accent)';
  } else {
    dEl.textContent = days;
    dEl.style.fontSize = '52px';
    dEl.style.color = days<=7 ? 'var(--accent)' : 'var(--text)';
    lEl.textContent = `${dayWord(days)} до дня рождения`;
    lEl.style.color = '';
  }

  const age = ageOnNextBirthday(p.date);
  aEl.textContent = age ? `Исполнится ${age} лет` : '';

  const giftEl = document.getElementById('d-gift-value');
  if (p.gift) {
    giftEl.className = 'info-card-value';
    giftEl.textContent = p.gift;
  } else {
    giftEl.className = 'info-card-empty';
    giftEl.textContent = 'Идей пока нет — добавь!';
  }

  const notifyStr = (p.notify || [1]).map(notifyLabel).join(', ');
  document.getElementById('d-notify-value').textContent = notifyStr;

  showScreen('detail');
}

// ════════════════════════════════════════
//  TELEGRAM
// ════════════════════════════════════════
function tgSendData(data) {
  try {
    if (window.Telegram?.WebApp) {
      window.Telegram.WebApp.sendData(JSON.stringify(data));
    }
  } catch(e) {}
}

// Init Telegram
try {
  const tg = window.Telegram?.WebApp;
  if (tg) {
    tg.ready();
    tg.expand();
    tg.enableClosingConfirmation();
    // Apply TG colors if available
    if (tg.colorScheme === 'light') {
      document.documentElement.style.setProperty('--bg','#f5f5f7');
      document.documentElement.style.setProperty('--surface','#ffffff');
      document.documentElement.style.setProperty('--surface2','#f0f0f3');
      document.documentElement.style.setProperty('--text','#0f0f13');
      document.documentElement.style.setProperty('--text-muted','rgba(0,0,0,0.4)');
      document.documentElement.style.setProperty('--text-mid','rgba(0,0,0,0.6)');
      document.documentElement.style.setProperty('--border','rgba(0,0,0,0.08)');
    }
  }
} catch(e) {}

// ════════════════════════════════════════
//  INIT
// ════════════════════════════════════════
renderList();
</script>
</body>
</html>
